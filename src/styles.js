import { styled } from '@mui/material/styles';



export const AppStyle = styled('div')(({ theme }) => ({
    borderRadius: theme.shape.borderRadius,
    backgroundColor: "black",
    width: '100%',

}));
